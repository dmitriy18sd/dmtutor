{
  "name": "peerjs-server",
  "version": "1.0.0",
  "description": "PeerJS signaling server for video calls",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "peer": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
